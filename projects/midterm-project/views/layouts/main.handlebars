<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <link rel="stylesheet" href="https://stackpath.bootstrapcdn.com/bootstrap/4.4.1/css/bootstrap.min.css" integrity="sha384-Vkoo8x4CGsO3+Hhxv8T/Q5PaXtkKtu6ug5TOeNV6gBiFeWPGFN9MuhOf23Q9Ifjh" crossorigin="anonymous" />
    <link rel="stylesheet" type="text/css" href="/public/css/main.css" />
    <title>Video Games</title>
</head>
<body>
    {{> nav}}
    <div id="content">
        {{{body}}}
    </div>
    <script src="https://code.jquery.com/jquery-3.4.1.slim.min.js" integrity="sha384-J6qa4849blE2+poT4WnyKhv5vZF5SrPo0iEjwBvKU7imGFAV0wwj1yYfoRSJoZ+n" crossorigin="anonymous"></script>
    <script src="https://cdn.jsdelivr.net/npm/popper.js@1.16.0/dist/umd/popper.min.js" integrity="sha384-Q6E9RHvbIyZFJoft+2mJbHaEWldlvI9IOYy5n3zV9zzTtmI3UksdQRVvoxMfooAo" crossorigin="anonymous"></script>
    <script src="https://stackpath.bootstrapcdn.com/bootstrap/4.4.1/js/bootstrap.min.js" integrity="sha384-wfSDF2E50Y2D1uUdj0O3uMBJnjuUD4Ih7YwaYd1iqfktj0Uod8GCExl3Og8ifwB6" crossorigin="anonymous"></script>
    <script>
        $('#search').on('input', function() {
            // If the search is not empty
            if($(this).val()) {
                // Hide everything but the current results
                $('tbody tr').each(function() {
                    if($(this).children('#name').text().toLowerCase().startsWith($('#search').val().toLowerCase())) {
                        $(this).show();
                    } else {
                        $('#empty').hide();
                        $(this).hide();
                    }
                })
            } else {
                $('#empty').hide();
                $('tbody tr').show();
            }
            // No matches
            if($('tbody tr').toArray().every(x => $(x).is(":hidden"))) {
                $('#empty').text('There are no games with the name "' + $(this).val() + '"');
                $('#empty').show();
            }
        })
    </script>
</body>
</html>
